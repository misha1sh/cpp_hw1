#pragma once

namespace big_num::common {
    using TValue = unsigned int;
    using TLongValue = long long;
    using TVector = std::vector<TValue>;
    using TSign = int8_t;

    static const long long BASE = 1e9;
}  // big_num::common